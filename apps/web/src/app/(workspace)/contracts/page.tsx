import { SkeletonModule } from "@/components/placeholders";

export default function ContractsPage() {
  return (
    <SkeletonModule
      title="合同 IP 条款审查"
      description="预留合同文本/PDF 上传、结构化风险输出、修改建议与 PDF 渲染入口。"
      featureName="FEATURE_CONTRACT_REVIEW"
    />
  );
}

